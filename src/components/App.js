import React from 'react';
import SearchBar from './SearchBar';

const App = () => {
  return (
    <div class="ui container" style={{marginTop: '10px'}}>
    <SearchBar />
    </div>
  );
};

export default App;
